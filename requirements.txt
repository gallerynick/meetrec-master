# MeetRec Master - 运行时依赖
# 说明：精确锁定版本见 requirements.lock（pip freeze 生成，用于 CI 可复现构建）
# 推荐安装方式：pip install -e ".[dev,ui,asr,ai,keywords]"

# --- 配置与密钥 ---
cryptography>=46.0,<47
keyring>=25.4,<27

# --- UI（PySide6，非 Web）---
PySide6==6.11.2
sounddevice==0.5.6

# --- 本地 ASR（离线，faster-whisper 1.2.1 返回元组，见 docs/03 §2.2）---
faster-whisper==1.2.1
av==18.1.0
onnxruntime==1.30.0

# --- AI 服务商 ---
openai==2.36.0
anthropic==1.6.0

# --- 关键词纠错 ---
pypinyin==0.55.0
rapidfuzz==3.14.6
